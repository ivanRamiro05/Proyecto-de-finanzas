# Variables de entorno para backend Django en Railway
# Copia este archivo como .env para desarrollo local.
# En Railway define estas variables desde el panel.

DJANGO_SECRET_KEY=django-insecure-cambia-esto
DJANGO_DEBUG=1
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
DJANGO_CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
DJANGO_CSRF_TRUSTED_ORIGINS=
# DATABASE_URL se genera automáticamente al crear el plugin Postgres en Railway.
# Ejemplo local:
# DATABASE_URL=postgres://usuario:password@localhost:5432/finanzas
